<% if current_user.admin || current_user.role == "manager" %>
  <ul class="nav nav-tabs">
    <li class="nav-item">
      <%= link_to "Todos", users_path, class: "nav-link" %>
    </li>
    <li class="nav-item">
      <%= link_to "Ativos", active_users_path, class: "nav-link" %>
    </li>
    <li class="nav-item">
      <%= link_to "Inativos", inactive_users_path, class: "nav-link" %>
    </li>
  </ul>
<% end %>
<h1>Aqui vai existir um perfil.</h1>
<h2>com as informações do cadastro.</h2>
<h3><%= @user.first_name %></h3>
<p><%= @user.status %></p>
<%= link_to "Editar", edit_user_path(@user), class: "btn btn-secondary" %>
<%# if current_user.is_following?(@user.id) %>
  <%#= link_to "Unfollow", unfollow_user_path(@user.id), method: :post, remote: true, class: "btn btn-info" %>
<%# else %>
  <%#= link_to "Follow", follow_user_path(@user.id), method: :post, remote: true, class: "btn btn-success" %>
<%# end %>